# Lines starting with this character are ignored
# To override some options specifically for the shared build, create a ffmpeg_options_shared.txt file.

# Basic built-in options, can be removed if you delete "--disable-autodetect"
--disable-autodetect
--enable-bzlib
--enable-iconv
--enable-lzma
--enable-schannel
--enable-zlib

# Hardware decoders
--enable-ffnvcodec
--enable-nvdec
--enable-d3d11va
--enable-amf

# Stuff we need
--enable-libdav1d
--enable-libaom
--enable-libwebp
--enable-libjxl
--disable-debug

# ffmpeg's native vp8/vp9 decoders do not contain alpha decoding support
--enable-libvpx

# for HDR support and tonemapping
--enable-vulkan
--enable-libplacebo